
## To run the project
```bash
#Compile
javac -d classes ContainerWithMostWater.java

#Run
java -cp classes ContainerWithMostWater

#Compile and Run
javac -d classes ContainerWithMostWater.java && java -cp classes ContainerWithMostWater
```
